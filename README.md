# kmr-worker

## development setup

Install depedencies:

```bash
yarn install
```

Add mongoDB database url to .env:

```
DATABASE_URL="<YOUR_CONNECTION_STRING>"
```

Start with:

```bash
yarn dev
```